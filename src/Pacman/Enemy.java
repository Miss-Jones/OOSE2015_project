package Pacman;
 
public class Enemy extends Entity {

	/**
	 * This is the enemy constructor
	 * @param x the integer: sets to the x position
	 * @param y the integer: sets to the y position
	 */
        Enemy(int x, int y){
                this.posX = x;
                this.posY = y;
        }      
}
